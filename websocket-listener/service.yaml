apiVersion: v1
kind: Service
metadata:
  name: websocket-listener
  labels:
    monitoring: enabled
spec:
  selector:
    app: websocket-listener
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
