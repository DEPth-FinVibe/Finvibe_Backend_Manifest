apiVersion: v1
kind: Service
metadata:
  name: profit-worker-golang
  labels:
    monitoring: enabled
spec:
  selector:
    app: profit-worker-golang
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
